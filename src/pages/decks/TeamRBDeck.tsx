import TeamPositionDeck from './TeamPositionDeck'
export default function TeamRBDeck() {
  return <TeamPositionDeck position="RB" deckIndex={3} title="Team — Running Backs"
    intro="How each team rations its backfield — carry distribution, rushing production, and the share of work each back commands. Pick a team in the rail to see its room." />
}
