import TeamPositionDeck from './TeamPositionDeck'
export default function TeamQBDeck() {
  return <TeamPositionDeck position="QB" deckIndex={2} title="Team — Quarterback"
    intro="How each team's quarterback production stacks up — passing volume, touchdowns, and fantasy output, live from the data and sliceable by the rail." />
}
